name: ormas-ci
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.11
  - numpy>=1.24
  - scipy>=1.10
  - pip
  - pip:
    - pyscf>=2.4
    - qdk-chemistry>=1.0.0
    - pytest>=7.0
    - pytest-cov>=4.0
    - ruff>=0.1
